# This file is just a holdover from the fact that we deploy from a hard-coded file name streamlit_app.py, so to avoid changing that for the time being we have this file, which invokes cicero
import cicero
cicero.main()
